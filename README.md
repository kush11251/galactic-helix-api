# Galactic Helix API
## Overview
Galactic Helix API is a scalable astronomical data processing and analysis system built using Python and the FastAPI framework.
## Installation
To install the required dependencies, run the following command:
pip install -r requirements.txt
## Usage
To start the API server, run the following command:
python src/main.py
